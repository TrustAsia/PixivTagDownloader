# Pixiv Downloader - 需求文档

## 1. 项目概述

本项目旨在开发一个Python应用程序，用于根据用户指定的Pixiv用户ID (UID) 下载其作品（包括图片、插画、漫画、小说）。程序需通过读取本地`cookie.txt`文件进行登录，并提供交互式界面供用户选择需要下载的作品标签。下载过程应采用多线程以提高效率，并加入随机延迟以避免触发Pixiv的访问频率限制。下载的内容需按照特定的目录结构和文件命名规则进行组织和保存，同时为图片/漫画类作品生成包含元数据的同名TXT文件，小说则直接保存为包含元数据和内容的TXT文件。项目强调模块化、高内聚、低耦合的设计原则。

## 2. 术语定义

*   **UID:** Pixiv用户的唯一标识符。
*   **PID:** Pixiv作品（图片、插画、漫画、小说）的唯一标识符。
*   **Cookie:** 用于用户身份验证和会话管理的小段文本数据。
*   **Tag:** 用户为作品添加的标签，用于分类和搜索。
*   **元数据 (Metadata):** 描述作品信息的数据，如标题、标签、描述、系列信息等。
*   **模块化:** 将程序分解为独立的、可重用的功能单元（模块）。
*   **高内聚:** 模块内部的元素（函数、类、数据）联系紧密，共同完成一个明确的任务。
*   **低耦合:** 模块之间的依赖关系尽可能少，一个模块的修改对其他模块的影响最小。

## 3. 功能需求 (Functional Requirements)

### 3.1 用户认证

*   **FR-AUTH-01:** 程序启动时，必须能够读取当前目录下名为 `cookie.txt` 的文件。
*   **FR-AUTH-02:** `cookie.txt` 文件格式应为 `key1=value1; key2=value2; ...` (分号后可能跟一个空格，也可能不跟，需兼容)。
*   **FR-AUTH-03:** 程序需使用读取到的Cookie信息模拟登录Pixiv账户，以获取访问用户作品的权限。
*   **FR-AUTH-04:** 如果 `cookie.txt` 文件不存在或格式不正确或Cookie失效，程序应提示错误信息并退出或提示用户重新提供。

### 3.2 用户交互

*   **FR-UI-01:** 程序成功登录后，应交互式提示用户输入目标用户的Pixiv UID。
*   **FR-UI-02:** 程序需验证用户输入的UID格式是否有效（纯数字）。若无效，应提示错误并要求重新输入。
*   **FR-UI-03:** 获取指定UID用户的所有作品（图片、插画、漫画、小说）后，程序需提取这些作品包含的所有唯一Tag。
*   **FR-UI-04:** 程序应向用户展示所有提取到的Tag列表。
*   **FR-UI-05:** 程序应允许用户交互式选择一个或多个Tag作为过滤条件。
*   **FR-UI-06:** 程序应支持用户选择过滤逻辑：“且”（AND - 作品需同时包含所有选定Tag）或“或”（OR - 作品只需包含任意一个选定Tag）。（ *注：根据复杂性，初期可只实现OR逻辑，后续迭代支持AND* ）
*   **FR-UI-07:** 用户完成Tag选择（或选择下载全部，不进行Tag过滤）后，程序应开始执行下载任务。

### 3.3 数据获取与处理

*   **FR-DATA-01:** 根据用户输入的UID，程序需要能获取该用户的基本信息，特别是 **用户名 (Username)**，用于后续文件路径创建。
*   **FR-DATA-02:** 程序需要能获取指定UID用户发布的所有作品列表，包含作品类型（图片、插画、漫画、小说）、PID、标题、标签、系列信息（如果存在）、图片URL（对于图片/漫画）、小说内容（对于小说）等元数据。
*   **FR-DATA-03:** 程序需根据用户选择的Tag和逻辑（AND/OR）过滤作品列表，确定最终需要下载的作品。
*   **FR-DATA-04:** 对于漫画或包含多张图片的作品（同一个PID下有多页），程序需要能获取所有页面的图片URL。

### 3.4 下载功能

*   **FR-DL-01:** 程序应使用多线程（或异步IO）并发下载符合条件的作品，以提高下载效率。线程/并发数量应可配置或根据系统资源自动调整（可选）。
*   **FR-DL-02:** 在每次网络请求（获取作品信息、下载文件等）之间，程序应加入一个随机时间延迟（例如，1-3秒之间），以模拟人类行为，降低被Pixiv服务器阻止的风险。延迟范围应可配置（可选）。
*   **FR-DL-03:** 下载过程中应处理可能发生的网络错误（如超时、连接失败），并进行适当的重试（可选）或记录错误并跳过。
*   **FR-DL-04:** 程序应能处理Pixiv的反爬虫机制，如需要设置合适的User-Agent、Referer等HTTP头。

### 3.5 文件存储与组织

*   **FR-STORE-01:** 所有下载的内容应保存在程序运行目录下的 `Output` 文件夹内。
*   **FR-STORE-02:** `Output` 文件夹下，应为每个目标用户创建一个子目录，命名格式为 `用户ID_用户名` (例如: `1234567_PixivUser`)。
*   **FR-STORE-03:** 在 `用户ID_用户名` 目录下，应根据作品类型创建子目录，例如 `Images` (图片/插画), `Manga` (漫画), `Novels` (小说)。
*   **FR-STORE-04:** 在类型目录下，应根据作品是否属于系列 (Series) 创建子目录：
    *   如果作品属于某个系列，则创建以系列名称命名的子目录。
    *   如果作品不属于任何系列，则创建名为 `无系列` (No_Series) 的子目录。
*   **FR-STORE-05 (图片/插画/漫画):**
    *   **单张图片作品:** 图片文件直接保存在 `系列` (或 `无系列`) 目录下。
    *   **多张图片作品 (同一PID):** 在 `系列` (或 `无系列`) 目录下创建一个以 `PID_作品标题` (或仅 `PID`，如果标题获取困难或包含非法字符) 命名的子目录，并将该PID下的所有图片保存在此子目录中。
    *   **文件命名:** 图片文件命名格式为 `PID_p索引号_作品标题.原始后缀名` (例如 `98765432_p0_MyArtwork.jpg`, `98765432_p1_MyArtwork.jpg`)。对于单张图片的作品，索引号为`p0`。文件名中的非法字符需要被替换或移除。
    *   **元数据文件:** 对于每个下载的图片/漫画PID，在其最终保存位置（单张图片所在目录，或多张图片的PID子目录）创建一个同名的 `.txt` 文件（例如 `98765432_p0_MyArtwork.txt` 或在PID目录下创建 `98765432_metadata.txt`），包含以下元数据：
        *   标题 (Title)
        *   作者UID (Author UID)
        *   作者用户名 (Author Username)
        *   作品PID (Artwork PID)
        *   标签 (Tags)，以逗号或分号分隔
        *   作品描述 (Description)
        *   系列信息 (Series Title / Series ID)，如果存在
        *   原始图片URL (Optional)
*   **FR-STORE-06 (小说):**
    *   **文件保存:** 小说保存在对应的 `Novels/系列` (或 `Novels/无系列`) 目录下。
    *   **文件命名:** 小说文件命名格式为 `PID_小说标题.txt`。文件名中的非法字符需要被替换或移除。
    *   **文件内容:** TXT文件内部应包含以下信息：
        *   标题 (Title): [小说标题]
        *   作者UID (Author UID): [作者UID]
        *   作者用户名 (Author Username): [作者用户名]
        *   小说PID (Novel PID): [小说PID]
        *   标签 (Tags): [tag1, tag2, tag3]
        *   系列信息 (Series): [系列标题 / 系列ID] (如果存在)
        *   描述 (Description): \n[小说描述内容]
        *   --- (分隔符) ---
        *   正文 (Content): \n[小说正文内容]
*   **FR-STORE-07:** 程序在创建目录和文件前应检查是否存在，避免覆盖，或提供选项让用户选择是否覆盖。
*   **FR-STORE-08:** 文件名和路径中应处理特殊字符和过长的问题，确保在不同操作系统下的兼容性。

## 4. 非功能需求 (Non-Functional Requirements)

*   **NFR-DESIGN-01 (模块化):** 代码结构应清晰地划分为不同的模块（如：认证模块、API交互模块、下载模块、文件处理模块、UI模块）。
*   **NFR-DESIGN-02 (高内聚):** 每个模块应专注于完成单一的功能（如：认证模块只负责登录和Cookie管理）。
*   **NFR-DESIGN-03 (低耦合):** 模块之间的依赖应最小化，通过接口或标准数据格式进行交互，方便独立修改和测试。
*   **NFR-PERF-01 (性能):** 使用多线程/并发下载以提升下载速度。
*   **NFR-ROBUST-01 (健壮性):** 程序应能优雅地处理常见的错误，如网络中断、无效输入、API变更（尽可能）、文件读写权限问题等，并提供有意义的错误提示。
*   **NFR-SEC-01 (安全性):** 避免硬编码敏感信息（如Cookie），从外部文件读取。
*   **NFR-USABILITY-01 (易用性):** 交互式命令行界面应清晰、直观，易于用户理解和操作。
*   **NFR-MAINTAIN-01 (可维护性):** 代码应遵循良好的编程规范（如PEP 8），添加必要的注释，易于理解和维护。
*   **NFR-COMPLIANCE-01 (合规性):** 加入随机延迟，避免对Pixiv服务器造成过大负担，和使用条款。程序仅供个人学习和备份用途。
*   配置文件支持，允许用户自定义输出路径、线程数、延迟等。
*   打包为可执行文件，方便非Python环境用户使用。
*   使用VirtualEnv创建虚拟环境运行。

## 5. 输入与输出

*   **输入:**
    *   `cookie.txt` 文件 (包含有效的Pixiv Cookie)
    *   用户通过命令行输入的 Pixiv UID
    *   用户通过命令行选择的 Tag (或选择不筛选)
    *   用户通过命令行选择的 Tag 过滤逻辑 (AND/OR)
*   **输出:**
    *   下载的图片/插画/漫画文件 (原始格式)
    *   为每个图片/漫画PID生成的元数据 `.txt` 文件
    *   下载的小说 `.txt` 文件 (包含元数据和内容)
    *   按照指定规则组织的目录结构 (`Output/用户ID_用户名/类型/系列/作品子目录(可选)/文件`)
    *   程序运行过程中的状态信息、进度提示和错误消息（打印到控制台）

## 6. 约束与假设

*   **CON-ENV-01:** 程序需要运行在安装了Python 3.x 的环境中。
*   **CON-LIB-01:** 需要依赖外部库，如 `requests` (用于HTTP请求), 可能需要 `beautifulsoup4` 或类似库进行HTML解析（如果API不直接提供所有信息）。
*   **CON-NET-01:** 需要稳定的互联网连接才能访问Pixiv。
*   **CON-PIXIV-01:** 依赖于当前Pixiv网站结构和（非官方）API的稳定性。如果Pixiv进行重大更新，可能需要修改程序。
*   **CON-COOKIE-01:** 假设用户提供的`cookie.txt`是有效的、未过期的，并且具有访问目标用户作品的权限。
*   **CON-RATE-01:** 随机延迟和多线程数量需要平衡效率和被阻止的风险，不能保证100%不触发访问限制。
